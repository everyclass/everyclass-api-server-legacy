[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=0.12.3"
gitpython = ">=2.1.8"
raven = ">=6.9.0"
elastic-apm = {version = "*", extras = ["flask"]}
pymysql = "*"
logbook = "*"
dbutils = "==1.3"

# this is just to avoid warning from `requests` package. Though there shouldn't have anything using this package.
urllib3 = "==1.23"

[dev-packages]
coverage = "==4.4.2"
codecov = "*"
nose = "==1.3.7"

[requires]
python_version = "3.6"
